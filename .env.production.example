# Copy to .env on the NAS next to docker-compose.yml.
# docker compose will automatically read .env.

# Admin bootstrap (used for first-run admin account; change after first login)
ADMIN_EMAIL=admin@flowsnap.local
ADMIN_PASSWORD=changeMeNow
